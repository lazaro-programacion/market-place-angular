import { ActiveUser1Pipe } from './active-user1.pipe';

describe('ActiveUser1Pipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveUser1Pipe();
    expect(pipe).toBeTruthy();
  });
});
